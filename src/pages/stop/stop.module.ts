import { NgModule } from '@angular/core';
import { IonicPageModule } from 'ionic-angular';
import { StopPage } from './stop';

@NgModule({
  declarations: [
    StopPage,
  ],
  imports: [
    IonicPageModule.forChild(StopPage),
  ],
})
export class StopPageModule {}
